[tox]
skipsdist = True
envlist =
    ruff
    pylint
    py{312,313}-django{52,60}

[testenv]
deps =
    django52: Django==5.2
    django60: Django==6.0
    pytest
    pytest-clarity
    pytest-cov
    pytest-django
    django-extensions
    django-browser-reload
    django-debug-toolbar
allowlist_externals =
    ./manage.py
commands =
    pytest --cov --cov-report=xml
setenv =
    PYTHONPATH = {toxinidir}:{env:PYTHONPATH:}

[testenv:ruff]
# Empty list to not install all deps from base testenv
deps =
skip_install = true
allowlist_externals = uv
commands_pre =
    uv sync --all-extras
commands = 
    uv run ruff check .
    uv run ruff format --check .

[testenv:pylint]
# Empty list to not install all deps from base testenv
deps =
skip_install = true
allowlist_externals = uv
commands_pre =
    uv sync --all-extras
commands = uv run pylint --django-settings-module=tests.settings django_custom_error_views tests

[pytest]
addopts = --ds=tests.settings --reuse-db -ra
testpaths = tests
norecursedirs = .git */migrations/* .venv

[coverage:run]
branch = True
source = django_custom_error_views

omit =
    ./.venv/*
    */migrations/*
    */apps.py

[coverage:report]
fail_under = 95
